% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_rois.R
\name{.filter_rois}
\alias{.filter_rois}
\title{Filter overlapping rois between adjacent rois}
\usage{
.filter_rois(dat, rois)
}
\arguments{
\item{dat}{Data frame containing roi assignments.}

\item{rois}{Integer vector of length 2 indicating the adjacent rois
to compare (e.g., \code{c(1, 2)} or \code{c(3, 4)}).}
}
\value{
Filtered data frame with non-overlapping roi assignments.
}
\description{
Internal helper used to ensure that adjacent regions of interest (rois)
do not contain duplicate stimulus intensity values (\code{x}). If overlaps occur,
multiple duplicated values are split between the lower and upper ROI so
that each retained intensity value is assigned uniquely to one region. If
exactly one intensity value is shared, it is removed from both ROIs to keep
the adjacent ROIs symmetric.
}
\keyword{internal}
